# LingHe 交接文档（2026-09-07 更新版）

> 写给接手的 AI：主人是小鹤音形用户，要求**整句/接龙智能候选**。请先读完本文再动手。
> 项目路径：`E:\CCSpace\projects\2026\09\linghe`（git 仓库，远端 ThreeAurora/LingHe 私密）
> 本文在 2026-09-06 版基础上追加 2026-09-07 全天工作（§3、§4、§5 新增/更新）。

---

## 1. 环境（必读，踩过坑）

- **Python 必须用 `E:/miniconda3/python.exe`**（托管 3.13 嵌入式版无 tkinter）。
- 路径一律 `E:/...` 风格；git 用 `git -C "E:/CCSpace/projects/2026/09/linghe"`（/e/ 路径 Windows git 不认）。
- 启动：`run.bat`（源码模式）。打包：`bash build_exe.sh`（一键 PyInstaller → dist/LingHe/）。
- 裁判模型：`ai_llm/qwen25-05b-hf`（Qwen2.5-0.5B 判别式整句 logP，加载约 35s）。
- 底库加载约 30-45s（273 万词），探针请后台跑（init 约 105-110s）。

## 2. 架构一句话

击键 → 池装配（码表固频 + 底库简拼 + Viterbi 整句 + 锚点串接 + 贪心 max_match + 口语字链 char_chains）→ 统计排序 → **万象语法模型**（mmap 直读、微秒级，端侧 0.1s 智能首选，见 §4.1）→ LLM 裁判异步终审 → 刷新候选。

核心文件：`linghe.py`（Engine/钩子/UI/compute 候选装配）、`rerank.py`（StatReranker：viterbi/anchor/max_match/char_chains/score_word/_eval_sent/_clean_chain）、`dict_engine.py`（底库/索引/平翘容错/掺权）、`gram_model.py`（**2026-09-07 新增**，万象语法 mmap 查询）、`ai_llm_judge.py`（QwenJudge）、`cloud_judge.py`（火山豆包云端裁判）。

## 3. ⚠️ 当前 git 状态（接手第一件事）

**2026-09-07 的工作全部在工作区未提交**，先 `git -C <项目> diff` + `git status` 通读再动代码。改动清单：

- `linghe.py`（大改）：compute() 内新增/修改多处——
  1. **多窗口上下文**：语法模型只认尾缀搭配，「喷出了孢子」无词条时整段证据归零，藏在更早的「蘑菇→孢子」被中间词截断。改为 `gwin = [gctx] + ctx_prevs[:3]` 逐上文词查窗取 max（§4.2 蘑菇喷出了bczi 案）。
  2. **整句音节守卫**（dyn 压顶）：只压「词库真词 + 打满整词音节」的词（`n_syl = n//2`），挡掉 viterbi 拼字伪句「不存在出」与异长词「菠菜甾醇」蹭证据（§4.4、§4.5）。
  3. **句内尾词语法证据**（**已实现并验证**）：整句候选的尾词由句内前词单独确证（蘑菇喷出了|孢子 压过 包子），封顶放宽 2×（§4.5，probe_bczi9 已验证蘑菇喷出了孢子 #1）。
  4. 整句候选筛选加入零孤字词链放行（`_clean_chain`，史诗级对决 案）。
  5. 修复动态调频日志格式化字符串。
- `rerank.py`：新增 `_clean_chain()`（零孤字词链：整句可拆成纯多字词典词，无单字残渣）+ `_cut_words()`（贪心最长词典词切分，仅贡献单字孤串信号，用于句子证据统计）。
- `config.json`：`max_buffer_len` 12 → 14（支持 14 键整句「蘑菇喷出了孢子」）。
- `gram_model.py`：**新增文件**（万象语法 mmap 直读，之前未入库）。
- `dict_engine.py` / `cloud_judge.py` / `dicts/user_dict.txt` / `linghe.log`：有未提交改动（部分为本次调参/日志）。
- `cache/tmp/`：新增 probe_bczi*.py 探针 7 个 + probe_epic/gram 系列（未入库，属探针工具，可留可清）。

**提交纪律（主人长期铁律）**：中文提交信息；一功能一提交；全量提交（产物/日志/过程文件都要入库）；md/日志/产物不单独提交，随功能提交一起上；禁止空提交。

## 4. 2026-09-07 新增机制（主人三个主诉的解法）

### 4.1 万象语法模型（gram_model.py）
- mmap 直读、查询微秒级，`gram.query(context, word, is_rear=False)` 返回搭配对数概率；`NC_PEN = -12.0` 为无证据基准，`ev = s - NC_PEN`。
- `is_rear`：word 后有 "$"（句尾）再比一轮 + rear_penalty；现有调用约定 `is_rear=(len(w)>=5)`（长词才按句尾查）。
- 用法（linghe.py compute()）：给动态池候选按上下文减价 `GRAM_ALPHA(0.25) × min(GRAM_MAX(12), ev)`，有搭配证据的词浮顶，码表固频区不受影响。

### 4.2 多窗口上下文（蘑菇喷出了bczi → 孢子 案）
- 病根：语法模型只认尾缀搭配，「喷出了孢子」无词条 → 整段证据归零；「蘑菇→孢子 ev17.3 / 喷出→孢子」藏在更早位置被中间词截断。
- 解法：`gwin = [gctx] + [w for w in ctx_prevs[:3] if w and w != gctx]`，对每个候选逐窗口查取 max ev。真搭配浮出（孢子 ev17.3 vs 包子 13.5），无关词窗零证据不反超。
- 验证：bczi + 蘑菇喷出了 + dyn开 → 孢子 第 1 位 ✅（probe_bczi6）。

### 4.3 零孤字词链（uiuijidvjt → 史诗级对决 案）
- 病根：整句闸（EV_SENT_GOOD=0.55）只认口语 3gram 证据，5 字短语中间跨词 3gram（史级对）天然是词间残渣，证据分结构性上不了 0.55——真短语（史诗级对决 ev0）与拼字伪句（是十几对绝 ev0）同分被挡。
- 解法：`_clean_chain(s)` = 整句可贪心切分成纯多字词典词（史诗级|对决），无单字残渣。只对整句闸放行（进候选池尾部），不替代统计排序、不进裁判名单。
- 验证：uiuijidvjt → 史诗级对决 在候选第 4 位（进了池，主人诉求是"能打出来"）✅（probe_bczi6）。

### 4.4 整句音节守卫（不存在出/菠菜甾醇 伪句压顶 案）
- 病根：dyn 压顶用上下文证据把最高候选提到固频前，viterbi 拼字伪句「不存在出」（靠 存在→不存在 类词条蹭 ev17.6）与异长词「菠菜甾醇」（4 音节蹭 蘑菇→甾醇 证据）混进来抢首位。
- 解法：`real_gev = {w: ev for w,ev in gev.items() if w in word_py and 音节数 in (n_syl, n_syl+n%2)}`，`n_syl = n//2`（用户 4 键双拼只打满 2 音节，只压 2 音节真词）。压顶门槛 `GRAM_TOP_EV=6.0`。
- 验证：bczi + 蘑菇喷出了 + dyn开 → 孢子 #1，不存在出/菠菜甾醇 不在前排 ✅（probe_bczi6）。

### 4.5 整句一次性识别（mogupfiulebczi → 蘑菇喷出了孢子，2026-09-07 晚 已验证）
- 病根（改前）：全双拼 14 键候选第 3 位（包子 #1、豹子 #2、孢子 #3），dyn 开/关同结果。原因：整句候选走统计排序，包子权重高（288537 vs 孢子 5551）压过孢子；语法证据被 GRAM_MAX=12 封顶后 孢子(ev17.3)/包子(ev13.5) 同压到 0.25×12=3.0，分不出先后。
- 修复（已实施并验证）：句内尾词语法证据——对 `len>=5 且 _clean_chain` 的整句候选，用句内前词当窗口查尾词：`iwins = 去重(words[:-1])`，`iev = max(gram(c, tail)) - NC_PEN`，`gboost[w] = max(已有, 0.25 × min(GRAM_MAX*2, iev))`。封顶放宽 2×（整句内部排序是统计地界，证据差须完整反映）。
- 诊断数据（probe_bczi8 实测）：蘑菇→孢子 ev17.3、蘑菇→包子 13.5、豹子/宝子 0；包子(-13.121)→豹子(-12.729)→孢子(-12.556)，差距 0.565/音节。
- **验证结果（probe_bczi9）**：mogupfiulebczi → 蘑菇喷出了孢子 **第 1 位**（dyn 开/关都 #1）✅；bczi+蘑菇喷出了 dyn开 → 孢子 第 1 ✅；uiui+这是一部 dyn开 → 史诗 第 1 ✅（回归无损）；不存在出/菠菜甾醇 不进前排 ✅。
- 注意：史诗级对决 在 uiuijidvjt 下仍第 4 位——句内证据未触发，因 `_cut_words('史诗级对决')` 切不出「史诗级」（不在词典或 weight=0），`_clean_chain` 为 False 不进句内证据通道。主人诉求（能打出来）已满足；若要升第 1，需把「史诗级」以词入库或放宽句内通道对 3+2 拆分的判定。

## 5. 主人验收用例全表（2026-09-07 更新）

| # | 码 | 期望 | 键制 | 当前状态 | 病根/备注 |
|---|------|------|------|---------|----------|
| 1 | wjtxixhcy | 我今天想吃西湖醋鱼 | 双拼简 9 键 | ✅ 排名 1 | 已修复（09-06） |
| 2 | nwgngdxcuyx | 那我给你个东西测试一下 | 双拼简 11 键 | 🔶 排名 2 | 裁判排 2，查 top1 与分差 |
| 3 | wilygbz | 我吃了一个包子 | 双拼简 7 键 | ❌ 缺席 | 见 §5.1（旧遗留） |
| 4 | uiuijidvjt | 史诗级对决 | 双拼简 9 键 | ✅ 第 4 位（进池） | 零孤字词链放行（§4.3）；句内证据未触发（史诗级未成词），升第 1 属可选优化 |
| 5 | bczi（上文 蘑菇喷出了） | 孢子 | 双拼简 4 键 | ✅ 第 1（dyn开） | 多窗口上下文 + 音节守卫（§4.2/4.4） |
| 6 | mogupfiulebczi | 蘑菇喷出了孢子 | 全双拼 14 键 | ✅ **第 1**（dyn开/关） | 句内尾词证据（§4.5），probe_bczi9 已验证 |
| 7 | uiui（上文 这是一部） | 史诗 | 双拼简 4 键 | ✅ 第 1（dyn开） | gram 10.3/ev22 压顶，回归不可破坏 |
| 8 | 我今天吃了一个+bz | 包子 | 接龙 2 键 | ✅ 裁判链 rank 1 | 09-06 已修 |
| 9 | 诗仙+lb / 诗圣+df | 李白 / 杜甫 | 接龙 2 键 | ✅ rank 1 | |
| 10 | wztwsmsh / wztwsmuh | 我昨天晚上没睡好 | 双拼简 8 键 | ❓ 未重测 | 名额修复后待测 |
| 11 | bydxwhm | 不要丢下我好吗 | 双拼简 7 键 | ❓ 未重测 | 同上 |
| 12 | wmncq / wmniq | 武媚娘传奇 | 双拼简 5 键 | ❌ 缺席 | 底库无此词（weight 0），数据问题 |
| 13 | tjz | 统计中 | 双拼简 3 键 | ❓ 未测 | 权重 50 偏低 |

## 6. 遗留问题与修复方向（按优先级）

### 6.1 整句一次性识别「蘑菇喷出了孢子」→ 第 1（§4.5，**已解决并验证**）
- probe_bczi9 已确认：mogupfiulebczi → 蘑菇喷出了孢子 第 1（dyn 开/关均 #1），且 bczi+蘑菇喷出了→孢子 #1、uiui+这是一部→史诗 #1、不存在出/菠菜甾醇 不进前排，全部回归无损。
- 可选优化（非必需）：史诗级对决 在 uiuijidvjt 下仍第 4 位，因「史诗级」不在词典切不出来（见 §4.5 注意）。主人只要"能打出来"，已满足；若要升第 1，把「史诗级」以词入库即可触发句内证据（史诗级→对决 ev22.3 实测很强）。

### 6.2 wilygbz 的「包子」尾巴（旧最大遗留）
- 尾巴证据全部不足：包 字不在 b 键口语字库前 20；sp2(包子)=465 vs sp2(不在)=26794；无屏幕上文时 score_word 压不过部长/不足。
- 建议方向（未实施）：char_chains 变体选尾词时把**头链末词（"一个"）作为句内上文**传给 score_word；或对 sp2(头末字+尾首字) 加权。

### 6.3 待办
1. ✅ 整句一次性识别已解决（probe_bczi9 验证通过，§4.5/§6.1）。
2. 重测 wztwsmsh/wztwsmuh/bydxwhm/tjz（probe_real_chain.py 加 case）。
3. 全部通过后：`bash build_exe.sh`（含 torch 版 QwenJudge 完整内嵌）+ 双击冒烟 + `git commit/push`（按 §3 提交纪律，本次工作区改动多，建议按功能拆多条中文提交：gram 模型 / 多窗口上下文 / 音节守卫 / 零孤字词链 / 整句一次性识别 / max_buffer_len）。

## 7. 探针工具箱（cache/tmp/，均可后台跑，init 约 105-110s）

- `probe_bczi9.py` — **最新**：整句一次性识别终验 + 全量回归（§5 用例 4/5/6/7），**已跑通：蘑菇喷出了孢子 #1，回归无损**
- `probe_bczi8.py` — 整句诊断：句内窗口→尾词证据 + 各候选代价差（本次定案靠它）
- `probe_bczi6.py` / `probe_bczi7.py` — 孢子压顶 / 整句 14 键基线
- `probe_real_chain.py` — 首选：Engine 本体 + 裁判，三案（wilygbz/bz有上文/bz无上文）
- `probe_e2e_v5.py` — 端到端终验（nwgngdxcuyx/wjtxixhcy/wilygbz）
- `probe_owner_samples.py` — 主人样本批测（整句组/接龙组/py 全码）

跑法：`E:/miniconda3/python.exe cache/tmp/xxx.py`，后台跑。

## 8. 避坑清单（前人踩过的，别再踩）

1. **ctypes GetLastError 污染**——互斥误判导致"双击没反应"（须 `use_last_error=True` + `ctypes.get_last_error()`）。
2. **探针≠真实链路**：必须用 Engine 本体（probe_bczi6/7/9 都是），否则修了探针坏了真实。
3. **语法模型只认尾缀搭配**：查「喷出了孢子」这类中间截断词串证据归零，必须逐上文词查窗取 max（§4.2）。
4. **dyn 压顶前必须过滤**：词库真词 + 音节数 == 输入码长/2，否则拼字伪句/异长词蹭证据霸榜（§4.4）。
5. **整句排序封顶不能死用 GRAM_MAX**：同音词句会全顶到上限分不出先后，整句内部排序需放宽封顶（§4.5）。
6. **judge 就绪要 35s**：启动前半分钟打字走 RBT3 回退，别在这窗口测智能句。
7. rerank 的 cost 是**越小越好**（负数比较），排查排序矛盾先查符号方向。
8. 验收口径：部分用例主人明说"不需要首选，出现即可"（武媚娘传奇/阿哈哈鸡汤来咯）。
9. 一功能一 git 提交（主人长期纪律），中文前缀提交信息。
