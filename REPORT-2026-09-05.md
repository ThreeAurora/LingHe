# 灵鹤 LingHe 通宵冲刺报告（2026-09-05 00:15）

主人睡前的口谕：不要任何确认，一直做到完，最后交一份「拿不准但我先做了」的清单等你拍板。
本报告就是那份清单。**所有东西都已提交 GitHub（ThreeAurora/LingHe，远端 a797196），不满意随时可以改回来。**

---

## 一、一句话总结

豆包输入法的双层架构（检索 <1ms 打底 + 判别式重排提供"智能体感"）已在灵鹤**全部落地**，
LLM 退居长句异步兜底；上屏联想（空格连打出常用搭配）也已上线。本地实测 compute() 0.5~3ms，
联想 1.3ms，整句用例 9/10。没有任何一步需要花钱或联网 API。

## 二、这一夜做了什么（按提交顺序，全部一功能一提交）

| 提交 | 内容 |
|---|---|
| 12eaf27（更早） | Shift 修复：LL 钩子上报 0xA0/0xA1 不是 0x10，旧代码三态全失效。单击切中英/组码中上屏英文/大写不误切，齐测通过 |
| c5fe92d | **SIGHAN 预训练共现表**：msr+pku 分词语料（16MB）离线计数，词级 18.2 万对 + 字级 19.9 万对 → `dicts/cooc_pre.bin`。给重排器开箱即用的"语言常识" |
| 0da47dd | tune.py 修复：调参脚本漏载预训练表，补上后整句用例 9/10 |
| 79703e2 | **上屏联想（豆包式）**：上屏词即出下一词候选，空格直上并续链。20/20 用例语义合理（这个→问题、觉得→自己/很、已经→成为），均延迟 1.3ms |
| 0e26f73 | 用户动态调频数据更新 |
| a797196 | README 全面同步：双层架构现状、联想章节、键位、目录、限制 |

（此前的 c 段提交：底库 marshal 缓存、Viterbi 全拼/简拼合并、主引擎接入双层架构等，见 git log。）

## 三、性能实测（都跑到哪了）

- 码表查询：0.005~0.04ms；底库查询：0.002ms —— **第一层（检索）远快于豆包需求**；
- compute() 整体（含重排/整句/装配）：0.49~2.91ms，预算 100ms 内余量 30 倍+；
- 统计重排（第二层）：单次 <5ms，与豆包端侧判别模型同量级，但零依赖零模型文件；
- 联想 next_word：平均 1.26ms（100 次热查询）；
- 底库加载：22s → 19s（marshal 缓存），**已到反序列化地板**；后台线程加载，打字不受影响；
- AI（qwen2.5:1.5b CPU）：0.22~0.3s，只对 >4 码异步兜底，永不阻塞按键。

## 四、拿不准但先做了的（**拍板清单**）

1. **字级预训练共现被禁用（B_PRE_CHAR=0）**。实测单字搭配（"中→国"这种）太泛，
   加进打分反而把 9/10 拖到 8/10。数据保留在 cooc_pre.bin 里，想开改一个常数即可。
2. **联想默认开启**（`config.json` 的 `liaison: true`）。风险：上屏后空格不再是"打新字"而是
   "上屏联想词"——如果主人打字习惯是连击空格，可能误上屏。不喜欢就把 `liaison` 改 false，
   或告诉我改成"联想词显示但不抢空格"（空格照常透传，用 `;` 上屏联想）。
3. **简拼冷启动歧义（wilygbz 类）接受为次优解**。`wsmzgr` 这类短简拼 unigram 等价解太多，
   统计层无解（信息论上限），靠三层兜底：检索→统计整句→AI 异步精修，且越用越准。
4. **OpenSubtitles 字幕语料放弃**。多个镜像 404、下载量大、还要引 jieba，收益对比 SIGHAN
   增量边际。口语语料仍是底库短板，但这个坑不值得今夜填。
5. **cooc_pre.bin（9.4MB）直接入库**，没走 LFS。私有仓库无所谓，以后若开源再迁 LFS。
6. **AI 收紧为仅 >4 码触发**。之前每码都问 AI，浪费且多数被码表命中覆盖；现在短码完全
   静态层，长码才有 AI 顺延。想恢复旧行为改 `ai.enabled`。
7. **重排常数定为 A=1.0, L=12, P=4, U=3, B=4, B_PRE=1.5**（tune.py 在 10 个真实用例上扫出来的）。
   不同语料可能不是最优，改前跑 `tune.py --report`。
8. **翻页行为**：`=`/`Tab` 下一页（末页回卷第一页）、`-` 上一页；数字选字作用于当前页。
   回卷是我拍的，若想末页停在原地改 linghe.py 一行。
9. **candidate_pool=45**：翻页池大小，够 5 页。太浅/太深都只是常数。

## 五、已知限制（没变也没新坑）

- 管理员窗口钩子失效（UIPI）；DirectInput 游戏收不到注入；
- RTX 2060 驱动还是坏的（`CUDA_VISIBLE_DEVICES=-1` 在 run.bat 里，驱动修好删那行）；
- 无托盘图标、UI 刻意简陋（主人说过 UI 随意）；
- 简拼整句的 unigram 歧义上限（见拍板 3）。

## 六、下一步建议（醒来后可挑）

1. 用一天真实打字验证联想/调频手感（尤其拍板 2）；
2. RTX 2060 驱动修好 → AI 换 3b/7b 模型，长句兜底质量上一档；
3. 若要真"神经版"判别重排（BERT 双塔），统计版架构不用动，只换打分函数。
